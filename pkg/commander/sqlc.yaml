version: "2"
sql:
  - engine: "sqlite"
    queries: "./queries.sql"
    schema: "./migrations"
    gen:
      go:
        package: "queries"
        out: "./queries.gen"
        emit_empty_slices: true
        emit_sql_as_comment: true
        query_parameter_limit: 1
