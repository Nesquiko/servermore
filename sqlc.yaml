version: "2"
sql:
  - engine: "sqlite"
    queries: "./db/queries.sql"
    schema: "./pkg/commander/migrations"
    gen:
      go:
        package: "queries"
        out: "./pkg/queries.gen"
        emit_empty_slices: true
        emit_sql_as_comment: true
        query_parameter_limit: 1
